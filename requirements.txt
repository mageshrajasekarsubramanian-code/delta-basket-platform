# Step 1: Delta API Wrapper - Minimal Dependencies
# Database & FastAPI dependencies will be added in later steps

# HTTP & WebSocket (required for Delta client)
httpx>=0.24.0
websockets>=11.0

# Utilities
pyyaml>=6.0
python-dateutil>=2.8.0
